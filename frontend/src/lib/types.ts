export interface Topic {
  title_name: string;
  description: string;
  completed: string;
}

export interface Question {
  question: string,
  questionType: string,
  difficulty: string,
  constraints: string
}

